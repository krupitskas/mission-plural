{
  "name": "mission-plural",
  "version": "0.1.0",
  "description": "Multiplayer orbital mechanics in the browser",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "@webgpu/types": "^0.1.52"
  }
}
